# Environment
NODE_ENV=development
PORT=4000
HOST=0.0.0.0

# Security
FAKER_API_KEY=mongodb-seeder-internal-key-2026
FAKER_SECURITY_ENABLED=false

# Redis (optional - only needed for distributed abort signals)
REDIS_URL=redis://localhost:6379
REDIS_ENABLED=false

# Logging
LOG_LEVEL=info
